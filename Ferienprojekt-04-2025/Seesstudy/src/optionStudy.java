public class optionStudy {

    public optionStudy(){

    }
    public void reDirection(Login fup, UserPage up) {
    /*    Timetable tt = new Timetable();
        Scanner scanner = new Scanner(System.in);
        System.out.println("0=Studytimer starts 1=the programm ends");

        Studytimer st = new Studytimer();
        fup.setStudytimer(st);
        int userAnswer = scanner.nextInt();
        switch (userAnswer) {
            case 0 -> {
                System.out.println("The timer starts now for ending the session click 0");
                st.setTime(LocalTime.now());
                LocalTime time=LocalTime.now();
                int userEnd;
                do {
                    userEnd = scanner.nextInt();
                } while (userEnd != 0);
                st.setEndTime(LocalTime.now());
              -----  LocalTime enTime=LocalTime.now();
                st.setMyTime(time,enTime);

                System.out.println("You studied so much " + st.getMyTime());


                tt.insertuser(up.getUserName(), up.getUserPassword(), st.getMyTime());
            }
            case 1 -> {
            }
        }*/
    }



}

